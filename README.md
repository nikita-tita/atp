# ATP Platform - Платформа для верифицированной торговли авиатехникой

> **Специализированная площадка для торговли авиатехникой с жёстким контролем участников**

## 🚀 Быстрый старт

### Предварительные требования

- **Docker** >= 20.10
- **Docker Compose** >= 2.0
- **Make** (опционально, для удобства)
- **Node.js** >= 18 (для локальной разработки)
- **Go** >= 1.21 (для локальной разработки)
- **Python** >= 3.11 (для аналитического сервиса)

### Установка и запуск

1. **Клонирование и настройка:**
   ```bash
   git clone <repository-url>
   cd ATP
   make setup
   ```

2. **Настройка переменных окружения:**
   ```bash
   # Отредактируйте .env файл с вашими API ключами
   nano .env
   ```

3. **Запуск всех сервисов:**
   ```bash
   make up
   ```

4. **Проверка статуса:**
   ```bash
   make status
   make health
   ```

### Доступные сервисы

После запуска доступны следующие сервисы:

| Сервис | URL | Описание |
|--------|-----|----------|
| **Web App** | http://localhost:3000 | Основное веб-приложение |
| **Admin Panel** | http://localhost:3100 | Панель администратора |
| **API Gateway** | http://localhost | Единая точка входа для API |
| **Auth Service** | http://localhost:3001 | Сервис аутентификации |
| **RabbitMQ UI** | http://localhost:15672 | Управление очередями (admin/admin) |
| **MinIO Console** | http://localhost:9001 | Файловое хранилище (admin/admin123) |

## 📋 Архитектура

### Микросервисы

```
├── services/
│   ├── auth-service/          # Аутентификация и авторизация
│   ├── verification-service/  # Верификация пользователей
│   ├── marketplace-service/   # Торговая площадка
│   ├── analytics-service/     # Аналитика и отчеты
│   ├── notification-service/  # Уведомления (email, SMS)
│   └── payment-service/       # Платежи и подписки
```

### Инфраструктура

- **PostgreSQL** - Основная база данных
- **Redis** - Кэширование и сессии
- **Elasticsearch** - Поиск по объявлениям
- **ClickHouse** - Аналитические данные
- **RabbitMQ** - Асинхронная обработка
- **MinIO** - S3-совместимое файловое хранилище
- **NGINX** - API Gateway и load balancer

## 🛠️ Команды разработки

### Основные команды

```bash
# Первоначальная настройка
make setup

# Запуск всех сервисов
make up

# Запуск только инфраструктуры (БД, кэш и т.д.)
make up-infra

# Остановка всех сервисов
make down

# Перезапуск всех сервисов
make restart

# Перезапуск конкретного сервиса
make restart-service SERVICE=auth-service
```

### Мониторинг и логи

```bash
# Просмотр логов всех сервисов
make logs

# Логи конкретного сервиса
make logs-service SERVICE=auth-service

# Статус всех сервисов
make status

# Проверка health checks
make health
```

### База данных

```bash
# Выполнить миграции
make db-migrate

# Заполнить тестовыми данными
make db-seed

# Создать резервную копию
make db-backup

# Восстановить из резервной копии
make db-restore BACKUP=backup_20240116_120000.sql
```

### Разработка

```bash
# Открыть shell в контейнере
make shell SERVICE=auth-service

# Запустить тесты
make test

# Проверить код линтерами
make lint

# Отформатировать код
make format
```

### Очистка

```bash
# Очистить volumes и неиспользуемые образы
make clean

# Полная очистка (ОСТОРОЖНО!)
make clean-all
```

## 📚 Документация

### Структура проекта

```
ATP/
├── docs/                          # Документация
│   ├── ТЗ_Платформа_Авиаторговли.md
│   ├── ПРОЕКТ_ПРАВИЛА.md
│   ├── DEV_LOG.md
│   └── api/                       # API документация
├── services/                      # Микросервисы
│   ├── auth-service/             # Аутентификация
│   ├── verification-service/     # Проверка пользователей
│   ├── marketplace-service/      # Торговая площадка
│   ├── analytics-service/        # Аналитика
│   ├── notification-service/     # Уведомления
│   └── payment-service/          # Платежи
├── frontend/                     # Фронтенд приложения
│   ├── web-app/                  # Веб-приложение
│   └── admin-panel/              # Админ панель
├── infrastructure/               # Инфраструктура
│   ├── docker/                   # Docker конфигурации
│   ├── k8s/                      # Kubernetes манифесты
│   └── monitoring/               # Мониторинг
├── shared/                       # Общие компоненты
│   ├── utils/                    # Утилиты
│   ├── types/                    # Типы данных
│   └── config/                   # Конфигурации
├── scripts/                      # Скрипты развертывания
├── docker-compose.dev.yml        # Docker Compose для разработки
├── Makefile                      # Команды для разработки
└── README.md                     # Этот файл
```

### API Endpoints

#### Auth Service (порт 3001)
- `POST /api/auth/register` - Регистрация пользователя
- `POST /api/auth/login` - Вход в систему
- `POST /api/auth/refresh` - Обновление токена
- `GET /api/auth/profile` - Профиль пользователя

#### Marketplace Service (порт 3003)
- `GET /api/marketplace/listings` - Список объявлений
- `POST /api/marketplace/listings` - Создание объявления
- `GET /api/marketplace/search` - Поиск самолетов
- `POST /api/marketplace/reserve` - Бронирование клиента

#### Verification Service (порт 3002)
- `POST /api/verification/start` - Начать верификацию
- `GET /api/verification/{id}/status` - Статус верификации
- `POST /api/verification/{id}/documents` - Загрузка документов

## 🔧 Конфигурация

### Переменные окружения

Основные переменные настраиваются в файле `.env`:

```bash
# Внешние API
FAA_API_KEY=your_faa_api_key_here
EASA_API_KEY=your_easa_api_key_here
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key_here

# Уведомления
SENDGRID_API_KEY=SG.your_sendgrid_api_key_here
TWILIO_ACCOUNT_SID=your_twilio_account_sid_here

# Безопасность
JWT_SECRET=your_super_secret_jwt_key_minimum_32_characters
```

### Получение API ключей

#### FAA API Key
1. Перейдите на https://registry.faa.gov/
2. Зарегистрируйтесь как developer
3. Создайте API key для aircraft registry access

#### Stripe
1. Создайте аккаунт на https://stripe.com/
2. В Dashboard перейдите в "Developers" → "API keys"
3. Скопируйте тестовые ключи для разработки

#### SendGrid
1. Создайте аккаунт на https://sendgrid.com/
2. Перейдите в Settings → API Keys
3. Создайте новый API key с Full Access

## 🧪 Тестирование

### Запуск тестов

```bash
# Все тесты
make test

# Тесты конкретного сервиса
docker-compose exec auth-service npm test

# Интеграционные тесты
docker-compose exec auth-service npm run test:integration

# E2E тесты
npm run test:e2e
```

### Coverage

Минимальное покрытие тестами: **80%**

```bash
# Отчет по покрытию
docker-compose exec auth-service npm run test:coverage
```

## 🚀 Развертывание

### Staging

```bash
make deploy-staging
```

### Production

```bash
make deploy-production
```

### Kubernetes

```bash
# Применить манифесты
kubectl apply -f infrastructure/k8s/

# Проверить статус подов
kubectl get pods -n atp-platform
```

## 📊 Мониторинг

### Prometheus Metrics

Доступны на `http://localhost:9090`

### Grafana Dashboards

Доступны на `http://localhost:3001` (admin/admin)

### Health Checks

```bash
# Проверить здоровье всех сервисов
make health

# Проверить конкретный сервис
curl http://localhost:3001/health
```

## 🔒 Безопасность

### Основные принципы

- **TLS 1.3** для всех коммуникаций
- **JWT** токены с коротким временем жизни
- **RBAC** система авторизации
- **Rate limiting** на API Gateway
- **Encryption at rest** для чувствительных данных

### Compliance

- **GDPR** - обработка персональных данных
- **SOC 2** - безопасность инфраструктуры
- **PCI DSS** - обработка платежных данных

## 🤝 Вклад в разработку

### Процесс разработки

1. Создайте feature branch
2. Внесите изменения
3. Запустите тесты и линтеры
4. Создайте Pull Request
5. Пройдите code review

### Стандарты кода

- **TypeScript** для фронтенда и Node.js сервисов
- **Go** для высоконагруженных сервисов
- **Python** для аналитики и ML
- **ESLint** + **Prettier** для JavaScript/TypeScript
- **golangci-lint** для Go
- **Black** + **isort** для Python

## 📞 Поддержка

- **Документация**: [docs/](./docs/)
- **API Документация**: [docs/api/](./docs/api/)
- **Dev Log**: [DEV_LOG.md](./DEV_LOG.md)
- **Правила проекта**: [ПРОЕКТ_ПРАВИЛА.md](./ПРОЕКТ_ПРАВИЛА.md)

## 📄 Лицензия

[MIT License](LICENSE)

---

**ATP Platform** © 2024 